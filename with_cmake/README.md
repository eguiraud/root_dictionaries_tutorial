## Generating dictionaries with CMake

See the `CMakeLists.txt` file in this directory for a commented example setup.
You can run the example with the following shell commands, for example:

```bash
$ cmake . && cmake --build .
$ ./readwrite_twoints
Written TwoInts(1, 2)
Read TwoInts(1, 2)
```
